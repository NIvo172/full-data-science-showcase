# Test data

Store only small, deterministic fixtures used by automated tests in this directory.
Do not copy complete training or production datasets here. Data-science projects can
access these files through the `pytest-datadir` fixtures.

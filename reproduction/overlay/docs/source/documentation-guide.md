```{include} ../README.md
```

## Showcase documentation layer

The template-rendered {doc}`project command reference <command-reference>` is the command contract for this selected configuration. The project-owned {doc}`showcase workflow guide <showcase-workflows>` adds concrete sequences, fixture values, and expected outputs without duplicating or redefining that contract.

# References

Curated per-dataset or per-task reference files that are *used by* the
pipeline but not *produced by* it — golden labels, expected outputs,
baseline metrics, or fixed vocabulary. Keep these small and deterministic;
large or volatile reference material belongs under `data/external/` behind a
DVC remote.
